/*
 * Project home: https://github.com/jaxio/celerio-angular-quickstart
 * 
 * Source code generated by Celerio, an Open Source code generator by Jaxio.
 * Documentation: http://www.jaxio.com/documentation/celerio/
 * Modificado por CARLOS BAZALAR
 * Email: cbazalarlarosa@gmail.com
 * Template pack-angular:src/main/java/dto/EntityDTOService.java.e.vm
 */
package com.incloud.hcp.service.impl;

import com.incloud.hcp.common.enums.AppParametriaLabelEnum;
import com.incloud.hcp.common.enums.AppParametriaModuloEnum;
import com.incloud.hcp.common.excel.ExcelDefault;
import com.incloud.hcp.common.graph.GraphBarChart;
import com.incloud.hcp.common.graph.GraphBean;
import com.incloud.hcp.common.graph.GraphDataset;
import com.incloud.hcp.common.graph.GraphPieChart;
import com.incloud.hcp.domain.AppParametria;
import com.incloud.hcp.domain.FacContacto;
import com.incloud.hcp.domain.FacContacto_;
import com.incloud.hcp.domain.MtrSector;
import com.incloud.hcp.domain.response.FacContactoResponse;
import com.incloud.hcp.exception.ServiceException;
import com.incloud.hcp.repository.delta.AppParametriaDeltaRepository;
import com.incloud.hcp.repository.delta.FacContactoDeltaRepository;
import com.incloud.hcp.repository.delta.MtrSectorDeltaRepository;
import com.incloud.hcp.service.FacContactoService;
import com.incloud.hcp.service._framework.bean.BeanCargaMasivoDTO;
import com.incloud.hcp.service._framework.bean.BeanListaMasivoDTO;
import com.incloud.hcp.service._framework.impl.JPACustomServiceImpl;
import com.incloud.hcp.service.delta.MtrSectorDeltaService;
import com.incloud.hcp.service.requireNew.FacContactoRequireNewService;
import com.incloud.hcp.service.support.PageRequestByExample;
import com.incloud.hcp.utils.Constants;
import com.incloud.hcp.utils.PredicateUtils;
import org.apache.commons.beanutils.BeanUtils;
import org.apache.commons.lang.StringUtils;
import org.apache.poi.hssf.usermodel.HSSFRow;
import org.apache.poi.ss.usermodel.Cell;
import org.apache.poi.ss.usermodel.CellStyle;
import org.apache.poi.ss.usermodel.Row;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.context.i18n.LocaleContextHolder;
import org.springframework.data.domain.Example;
import org.springframework.data.domain.ExampleMatcher;
import org.springframework.data.domain.Sort;
import org.springframework.stereotype.Service;
import org.springframework.transaction.annotation.Propagation;
import org.springframework.transaction.annotation.Transactional;

import javax.persistence.criteria.CriteriaBuilder;
import javax.persistence.criteria.Predicate;
import javax.persistence.criteria.Root;
import java.math.BigDecimal;
import java.util.ArrayList;
import java.util.List;
import java.util.Optional;

/**
 * A simple DTO Facility for FacContacto.
 */
@Service
@Transactional(propagation = Propagation.REQUIRED, rollbackFor = Exception.class)
public abstract class FacContactoServiceImpl extends JPACustomServiceImpl<FacContactoResponse, FacContacto, Integer> implements FacContactoService {

    protected final String NAME_SHEET = "FacContacto";
    protected final String CONFIG_TITLE = "com/incloud/hcp/excel/FacContactoExcel.xml";
    private final Integer REGISTROS_COLOR = 10;

    @Autowired
    protected AppParametriaDeltaRepository appParametriaDeltaRepository;

    @Autowired
    protected FacContactoDeltaRepository facContactoDeltaRepository;

    @Autowired
    protected FacContactoRequireNewService facContactoRequireNewService;

    @Autowired
    protected MtrSectorDeltaService mtrSectorDeltaService;

    @Autowired
    protected MtrSectorDeltaRepository mtrSectorDeltaRepository;

    /*****************************/
    /* Metodos de Busqueda       */
    /*****************************/

    protected final ExampleMatcher setAbstractFind(ExampleMatcher matcher) {
        matcher = ExampleMatcher.matching() //
                .withMatcher(FacContacto_.usuario.getName(), match -> match.ignoreCase().startsWith())
                .withMatcher(FacContacto_.contactoNombres.getName(), match -> match.ignoreCase().startsWith())
                .withMatcher(FacContacto_.contactoApellidos.getName(), match -> match.ignoreCase().startsWith())
                .withMatcher(FacContacto_.email.getName(), match -> match.ignoreCase().startsWith())
                .withMatcher(FacContacto_.tipoContacto.getName(), match -> match.ignoreCase().startsWith())
                .withMatcher(FacContacto_.activo.getName(), match -> match.ignoreCase().startsWith())
                .withMatcher(FacContacto_.estado.getName(), match -> match.ignoreCase().startsWith())
                .withMatcher(FacContacto_.codigoIdp.getName(), match -> match.ignoreCase().startsWith());
        return matcher;
    }

    protected abstract Sort setFindAll(Sort sort);

    protected abstract Sort setFind(FacContacto req, ExampleMatcher matcher, Example<FacContacto> example, Sort sort);

    protected abstract void setFindPaginated(PageRequestByExample<FacContacto> req, ExampleMatcher matcher, Example<FacContacto> example);

    protected final FacContacto setObtenerBeanResponse(FacContactoResponse bean) {
        return bean.getBean();
    }

    protected final Class<FacContacto> setObtenerClassBean() {
        return FacContacto.class;
    }

    protected List<Predicate> setAbstractPredicate(FacContactoResponse bean, CriteriaBuilder cb, Root<FacContacto> root) {
        List<Predicate> predicates = new ArrayList<>();
        FacContacto entity = bean.getBean();
        /* Obtener condiciones */
        PredicateUtils.addPredicates(predicates, bean.getIdCondicion(), "id", entity.getId(), cb, root);
        PredicateUtils.addPredicates(predicates, bean.getUsuarioCondicion(), "usuario", entity.getUsuario(), cb, root);
        PredicateUtils.addPredicates(predicates, bean.getContactoNombresCondicion(), "contactoNombres", entity.getContactoNombres(), cb, root);
        PredicateUtils.addPredicates(predicates, bean.getContactoApellidosCondicion(), "contactoApellidos", entity.getContactoApellidos(), cb, root);
        PredicateUtils.addPredicates(predicates, bean.getEmailCondicion(), "email", entity.getEmail(), cb, root);
        PredicateUtils.addPredicates(predicates, bean.getTipoContactoCondicion(), "tipoContacto", entity.getTipoContacto(), cb, root);
        PredicateUtils.addPredicates(predicates, bean.getActivoCondicion(), "activo", entity.getActivo(), cb, root);
        PredicateUtils.addPredicates(predicates, bean.getEstadoCondicion(), "estado", entity.getEstado(), cb, root);
        PredicateUtils.addPredicates(predicates, bean.getCodigoIdpCondicion(), "codigoIdp", entity.getCodigoIdp(), cb, root);
        //PredicateUtils.addPredicates(predicates, bean.getMtrSectorCondicion(), "mtrSector", entity.getMtrSector(), cb, root);
        /* Obtener valores de Lista */
        PredicateUtils.addPredicatesListValorPrimitivo(predicates, "id", bean.getIdList(), cb, root);
        PredicateUtils.addPredicatesListValorPrimitivo(predicates, "usuario", bean.getUsuarioList(), cb, root);
        PredicateUtils.addPredicatesListValorPrimitivo(predicates, "contactoNombres", bean.getContactoNombresList(), cb, root);
        PredicateUtils.addPredicatesListValorPrimitivo(predicates, "contactoApellidos", bean.getContactoApellidosList(), cb, root);
        PredicateUtils.addPredicatesListValorPrimitivo(predicates, "email", bean.getEmailList(), cb, root);
        PredicateUtils.addPredicatesListValorPrimitivo(predicates, "tipoContacto", bean.getTipoContactoList(), cb, root);
        PredicateUtils.addPredicatesListValorPrimitivo(predicates, "activo", bean.getActivoList(), cb, root);
        PredicateUtils.addPredicatesListValorPrimitivo(predicates, "estado", bean.getEstadoList(), cb, root);
        PredicateUtils.addPredicatesListValorPrimitivo(predicates, "codigoIdp", bean.getCodigoIdpList(), cb, root);
        //PredicateUtils.addPredicatesListValorBean(predicates, "mtrSector", bean.getMtrSectorList(), cb, root);
        return predicates;
    }

    /****************/
    /* METODOS CRUD */
    /****************/

    protected final String validacionesPrevias(FacContacto bean) throws Exception {
        String mensaje = "";
        if (!Optional.of(bean.getUsuario()).isPresent()) {
            String msg = this.messageSource.getMessage("message.facContacto.usuario.requerido", null, LocaleContextHolder.getLocale());
            mensaje += "* " + msg + "<br/>";
        }
        if (!Optional.of(bean.getContactoNombres()).isPresent()) {
            String msg = this.messageSource.getMessage("message.facContacto.contactoNombres.requerido", null, LocaleContextHolder.getLocale());
            mensaje += "* " + msg + "<br/>";
        }
        if (!Optional.of(bean.getContactoApellidos()).isPresent()) {
            String msg = this.messageSource.getMessage("message.facContacto.contactoApellidos.requerido", null, LocaleContextHolder.getLocale());
            mensaje += "* " + msg + "<br/>";
        }
        if (!Optional.of(bean.getEmail()).isPresent()) {
            String msg = this.messageSource.getMessage("message.facContacto.email.requerido", null, LocaleContextHolder.getLocale());
            mensaje += "* " + msg + "<br/>";
        }
        String msg = this.setValidacionesPrevias(bean);
        if (StringUtils.isNotBlank(msg)) {
            mensaje += "* " + msg + "<br/>";
        }
        return mensaje;
    }

    protected String setValidacionesPrevias(FacContacto bean) throws Exception {
        String mensaje = "";
        return mensaje;
    }

    protected String validacionesPreviasCreate(FacContacto bean) throws Exception {
        String msg = null;
        FacContacto validar = null;
        validar = this.facContactoDeltaRepository.getByUsuario(bean.getUsuario());
        if (Optional.ofNullable(validar).isPresent()) {
            msg = this.messageSource.getMessage("message.facContacto.usuario.duplicado", null, LocaleContextHolder.getLocale());
            return msg;
        }
        validar = this.facContactoDeltaRepository.getByEmail(bean.getEmail());
        if (Optional.ofNullable(validar).isPresent()) {
            msg = this.messageSource.getMessage("message.facContacto.email.duplicado", null, LocaleContextHolder.getLocale());
            return msg;
        }
        return msg;
    }

    protected String validacionesPreviasSave(FacContacto bean) throws Exception {
        String msg = null;
        FacContacto validar = null;
        validar = this.facContactoDeltaRepository.getByUsuario(bean.getUsuario());
        if (Optional.ofNullable(validar).isPresent()) {
            if (bean.getId().intValue() != validar.getId().intValue()) {
                msg = this.messageSource.getMessage("message.facContacto.usuario.duplicado", null, LocaleContextHolder.getLocale());
                return msg;
            }
        }
        validar = this.facContactoDeltaRepository.getByEmail(bean.getEmail());
        if (Optional.ofNullable(validar).isPresent()) {
            if (bean.getId().intValue() != validar.getId().intValue()) {
                msg = this.messageSource.getMessage("message.facContacto.email.duplicado", null, LocaleContextHolder.getLocale());
                return msg;
            }
        }
        return msg;
    }

    protected FacContacto completarDatosBean(FacContacto bean) throws Exception {
        BigDecimal data = new BigDecimal(0.00);
        bean = this.setCompletarDatosBean(bean);
        return bean;
    }

    protected FacContacto setCompletarDatosBean(FacContacto bean) throws Exception {
        return bean;
    }

    protected final FacContacto setAbstractCreate(FacContacto dto) throws Exception {
        FacContacto bean = new FacContacto();
        bean = (FacContacto) BeanUtils.cloneBean(dto);
        return bean;
    }

    /************************/
    /* METODOS CRUD Masivos */
    /************************/

    protected FacContacto setUploadExcel(Cell currentCell, FacContacto facContacto, int contador) throws Exception {
        Double valorDouble = new Double(0);
        BigDecimal valorDecimal = new BigDecimal(0);
        Boolean valorBoolean = new Boolean(true);
        String valorCadena = "";
        switch (contador) {
        case 1:
            try {
                valorCadena = currentCell.getStringCellValue();
                if (valorCadena.length() > 25) {
                    throw new ServiceException("Valor Campo usuario contiene mas de 25 caracter(es)");
                }
                facContacto.setUsuario(valorCadena);
            } catch (Exception e) {
                throw new ServiceException("Valor Campo usuario está en formato incorrecto");
            }
            break;
        case 2:
            try {
                valorCadena = currentCell.getStringCellValue();
                if (valorCadena.length() > 50) {
                    throw new ServiceException("Valor Campo contactoNombres contiene mas de 50 caracter(es)");
                }
                facContacto.setContactoNombres(valorCadena);
            } catch (Exception e) {
                throw new ServiceException("Valor Campo contactoNombres está en formato incorrecto");
            }
            break;
        case 3:
            try {
                valorCadena = currentCell.getStringCellValue();
                if (valorCadena.length() > 50) {
                    throw new ServiceException("Valor Campo contactoApellidos contiene mas de 50 caracter(es)");
                }
                facContacto.setContactoApellidos(valorCadena);
            } catch (Exception e) {
                throw new ServiceException("Valor Campo contactoApellidos está en formato incorrecto");
            }
            break;
        case 4:
            try {
                valorCadena = currentCell.getStringCellValue();
                if (valorCadena.length() > 100) {
                    throw new ServiceException("Valor Campo email contiene mas de 100 caracter(es)");
                }
                facContacto.setEmail(valorCadena);
            } catch (Exception e) {
                throw new ServiceException("Valor Campo email está en formato incorrecto");
            }
            break;
        case 5:
            try {
                valorCadena = currentCell.getStringCellValue();
                if (valorCadena.length() > 3) {
                    throw new ServiceException("Valor Campo tipoContacto contiene mas de 3 caracter(es)");
                }
                facContacto.setTipoContacto(valorCadena);
            } catch (Exception e) {
                throw new ServiceException("Valor Campo tipoContacto está en formato incorrecto");
            }
            break;
        case 6:
            try {
                valorCadena = currentCell.getStringCellValue();
                if (valorCadena.length() > 1) {
                    throw new ServiceException("Valor Campo activo contiene mas de 1 caracter(es)");
                }
                facContacto.setActivo(valorCadena);
            } catch (Exception e) {
                throw new ServiceException("Valor Campo activo está en formato incorrecto");
            }
            break;
        case 7:
            try {
                valorCadena = currentCell.getStringCellValue();
                if (valorCadena.length() > 1) {
                    throw new ServiceException("Valor Campo estado contiene mas de 1 caracter(es)");
                }
                facContacto.setEstado(valorCadena);
            } catch (Exception e) {
                throw new ServiceException("Valor Campo estado está en formato incorrecto");
            }
            break;
        case 8:
            try {
                valorCadena = currentCell.getStringCellValue();
                if (valorCadena.length() > 20) {
                    throw new ServiceException("Valor Campo codigoIdp contiene mas de 20 caracter(es)");
                }
                facContacto.setCodigoIdp(valorCadena);
            } catch (Exception e) {
                throw new ServiceException("Valor Campo codigoIdp está en formato incorrecto");
            }
            break;
        default:
            break;
        }
        return facContacto;
    }

    protected AppParametria setObtenerRegistroConfiguracionUploadExcel() {
        AppParametria appParametriaData = this.appParametriaDeltaRepository.getByModuloAndLabelAndStatus(AppParametriaModuloEnum.CARGA_EXCEL.getEstado(),
                AppParametriaLabelEnum.INICIO_REGISTRO_DATA.getEstado(), Constants.ESTADO_ACTIVO);
        return appParametriaData;
    }

    protected FacContacto setInicializarBeanUpdateExcel() {
        FacContacto bean = new FacContacto();
        bean.setId(null);
        return bean;
    }

    protected final Integer setObtenerId(FacContacto bean) {
        return bean.getId();
    }

    /************************/
    /* Instancia de Bean    */
    /************************/

    protected final FacContacto createInstance() {
        FacContacto facContacto = new FacContacto();
        return facContacto;
    }

    protected final BeanCargaMasivoDTO<FacContacto> createInstanceMasivoDTO() {
        BeanCargaMasivoDTO<FacContacto> beanCargaMasivoDTO = new BeanCargaMasivoDTO<FacContacto>();
        return beanCargaMasivoDTO;

    }

    protected final BeanListaMasivoDTO<BeanCargaMasivoDTO<FacContacto>> createInstanceListaMasivoDTO() {
        BeanListaMasivoDTO<BeanCargaMasivoDTO<FacContacto>> beanCargaMasivoDTOBeanListaMasivoDTO = new BeanListaMasivoDTO<BeanCargaMasivoDTO<FacContacto>>();
        return beanCargaMasivoDTOBeanListaMasivoDTO;
    }

    /*****************************/
    /* Metodos que generan Excel */
    /*****************************/

    protected String devuelveNombreSheet() {
        return this.NAME_SHEET;
    }

    protected String devuelveListaHeaderExcelXML() {
        return this.CONFIG_TITLE;
    }

    protected int setAbstractDownloadExcel(FacContacto bean, HSSFRow dataRow) {
        int contador = 0;
        ExcelDefault.setValueCell(bean.getId(), dataRow.createCell(contador));
        contador++;
        ExcelDefault.setValueCell(bean.getUsuario(), dataRow.createCell(contador));
        contador++;
        ExcelDefault.setValueCell(bean.getContactoNombres(), dataRow.createCell(contador));
        contador++;
        ExcelDefault.setValueCell(bean.getContactoApellidos(), dataRow.createCell(contador));
        contador++;
        ExcelDefault.setValueCell(bean.getEmail(), dataRow.createCell(contador));
        contador++;
        ExcelDefault.setValueCell(bean.getTipoContacto(), dataRow.createCell(contador));
        contador++;
        ExcelDefault.setValueCell(bean.getActivo(), dataRow.createCell(contador));
        contador++;
        ExcelDefault.setValueCell(bean.getEstado(), dataRow.createCell(contador));
        contador++;
        ExcelDefault.setValueCell(bean.getCodigoIdp(), dataRow.createCell(contador));
        contador++;
        return contador;
    }

    protected int setAbstractDownloadExcelSXLSX(FacContacto bean, Row dataRow, List<CellStyle> cellStyleList) {
        int contador = 0;
        ExcelDefault.setValueCell(bean.getId(), dataRow.createCell(contador), "I", cellStyleList);
        contador++;
        ExcelDefault.setValueCell(bean.getUsuario(), dataRow.createCell(contador), "S", cellStyleList);
        contador++;
        ExcelDefault.setValueCell(bean.getContactoNombres(), dataRow.createCell(contador), "S", cellStyleList);
        contador++;
        ExcelDefault.setValueCell(bean.getContactoApellidos(), dataRow.createCell(contador), "S", cellStyleList);
        contador++;
        ExcelDefault.setValueCell(bean.getEmail(), dataRow.createCell(contador), "S", cellStyleList);
        contador++;
        ExcelDefault.setValueCell(bean.getTipoContacto(), dataRow.createCell(contador), "S", cellStyleList);
        contador++;
        ExcelDefault.setValueCell(bean.getActivo(), dataRow.createCell(contador), "S", cellStyleList);
        contador++;
        ExcelDefault.setValueCell(bean.getEstado(), dataRow.createCell(contador), "S", cellStyleList);
        contador++;
        ExcelDefault.setValueCell(bean.getCodigoIdp(), dataRow.createCell(contador), "S", cellStyleList);
        contador++;
        return contador;
    }

    protected String setAbstractGenerateInsertExcelSXLSX(FacContacto bean) {
        String fechaS = "";
        String sqlInsert = "INSERT INTO fac_contacto(";
        sqlInsert = sqlInsert + "fac_contacto_id" + ", ";
        sqlInsert = sqlInsert + "usuario" + ", ";
        sqlInsert = sqlInsert + "contacto_nombres" + ", ";
        sqlInsert = sqlInsert + "contacto_apellidos" + ", ";
        sqlInsert = sqlInsert + "email" + ", ";
        sqlInsert = sqlInsert + "tipo_contacto" + ", ";
        sqlInsert = sqlInsert + "activo" + ", ";
        sqlInsert = sqlInsert + "estado" + ", ";
        sqlInsert = sqlInsert + "codigo_idp" + ", ";
        sqlInsert = sqlInsert + "mtr_sector_id" + ")";
        sqlInsert = sqlInsert + " VALUES (";
        sqlInsert = sqlInsert + bean.getId() + ", ";
        if (StringUtils.isBlank(bean.getUsuario())) {
            sqlInsert = sqlInsert + "null" + ", ";
        } else {
            sqlInsert = sqlInsert + "'" + bean.getUsuario() + "'" + ", ";
        }
        if (StringUtils.isBlank(bean.getContactoNombres())) {
            sqlInsert = sqlInsert + "null" + ", ";
        } else {
            sqlInsert = sqlInsert + "'" + bean.getContactoNombres() + "'" + ", ";
        }
        if (StringUtils.isBlank(bean.getContactoApellidos())) {
            sqlInsert = sqlInsert + "null" + ", ";
        } else {
            sqlInsert = sqlInsert + "'" + bean.getContactoApellidos() + "'" + ", ";
        }
        if (StringUtils.isBlank(bean.getEmail())) {
            sqlInsert = sqlInsert + "null" + ", ";
        } else {
            sqlInsert = sqlInsert + "'" + bean.getEmail() + "'" + ", ";
        }
        if (StringUtils.isBlank(bean.getTipoContacto())) {
            sqlInsert = sqlInsert + "null" + ", ";
        } else {
            sqlInsert = sqlInsert + "'" + bean.getTipoContacto() + "'" + ", ";
        }
        if (StringUtils.isBlank(bean.getActivo())) {
            sqlInsert = sqlInsert + "null" + ", ";
        } else {
            sqlInsert = sqlInsert + "'" + bean.getActivo() + "'" + ", ";
        }
        if (StringUtils.isBlank(bean.getEstado())) {
            sqlInsert = sqlInsert + "null" + ", ";
        } else {
            sqlInsert = sqlInsert + "'" + bean.getEstado() + "'" + ", ";
        }
        if (StringUtils.isBlank(bean.getCodigoIdp())) {
            sqlInsert = sqlInsert + "null" + ", ";
        } else {
            sqlInsert = sqlInsert + "'" + bean.getCodigoIdp() + "'" + ", ";
        }
        sqlInsert = sqlInsert + bean.getMtrSector().getId();
        sqlInsert = sqlInsert + " );";
        return sqlInsert;
    }

    /*****************/
    /* Otros Metodos */
    /*****************/

    public Long countByMtrSector(MtrSector mtrSector) {
        return this.facContactoDeltaRepository.countByMtrSector(mtrSector);
    }

    public GraphBean graphByMtrSector() {
        List<MtrSector> lista = this.mtrSectorDeltaRepository.findAll();
        if (lista == null || lista.size() <= 0) {
            return null;
        }
        GraphBean graphBean = new GraphBean();

        /* Obteniendo Pie Chart */
        GraphPieChart graphPieChart = new GraphPieChart();
        GraphDataset graphDataset = new GraphDataset();
        GraphDataset[] listaGraphDataset = new GraphDataset[1];
        Long[] data = new Long[lista.size()];
        String[] labels = new String[lista.size()];
        String[] backgroundColor = new String[lista.size()];
        String[] hoverBackgroundColor = new String[lista.size()];
        String[] borderColor = new String[lista.size()];
        String[] hoverBorderColor = new String[lista.size()];
        int contador = 0;
        int contadorColor = 0;
        for (MtrSector bean : lista) {
            Long valor = this.facContactoDeltaRepository.countByMtrSector(bean);
            String descripcion = this.setGraphDescripcionByMtrSector(bean);
            data[contador] = valor;
            labels[contador] = descripcion;
            backgroundColor[contador] = Constants.BACKGROUND_COLOR_GRAPH[contadorColor];
            hoverBackgroundColor[contador] = Constants.HOVER_BACKGROUND_COLOR_GRAPH[contadorColor];
            borderColor[contador] = Constants.BACKGROUND_COLOR_GRAPH[contadorColor];
            hoverBorderColor[contador] = Constants.HOVER_BACKGROUND_COLOR_GRAPH[contadorColor];
            contador++;
            contadorColor++;
            if (contadorColor >= REGISTROS_COLOR) {
                contadorColor = 1;
            }
        }
        String titulo = this.setGraphPieChartTituloByMtrSector();
        graphDataset.setLabel(titulo);
        graphDataset.setData(data);
        graphDataset.setBackgroundColor(backgroundColor);
        graphDataset.setHoverBackgroundColor(hoverBackgroundColor);
        listaGraphDataset[0] = graphDataset;
        graphPieChart.setDatasets(listaGraphDataset);
        graphPieChart.setLabels(labels);
        graphBean.setPiechart(graphPieChart);

        GraphBarChart graphBarChart = new GraphBarChart();
        GraphDataset[] listaGraphDatasetBar = new GraphDataset[lista.size()];
        String[] labelsBar = new String[1];
        labelsBar[0] = this.setGraphBarChartTituloByMtrSector();
        contador = 0;
        contadorColor = 0;
        for (MtrSector bean : lista) {
            Long valor = this.facContactoDeltaRepository.countByMtrSector(bean);
            String descripcion = this.setGraphDescripcionByMtrSector(bean);
            GraphDataset graphDatasetBar = new GraphDataset();
            Long[] dataBar = new Long[1];

            String[] backgroundColorBar = new String[1];
            String[] hoverBackgroundColorBar = new String[1];
            String[] borderColorBar = new String[1];
            String[] hoverBorderColorBar = new String[1];

            dataBar[0] = valor;
            backgroundColorBar[0] = Constants.BACKGROUND_COLOR_GRAPH[contadorColor];
            hoverBackgroundColorBar[0] = Constants.HOVER_BACKGROUND_COLOR_GRAPH[contadorColor];
            borderColorBar[0] = Constants.BACKGROUND_COLOR_GRAPH[contadorColor];
            hoverBorderColorBar[0] = Constants.HOVER_BACKGROUND_COLOR_GRAPH[contadorColor];

            graphDatasetBar.setLabel(descripcion);
            graphDatasetBar.setData(dataBar);
            graphDatasetBar.setBackgroundColor(backgroundColorBar);
            graphDatasetBar.setHoverBackgroundColor(hoverBackgroundColorBar);
            graphDatasetBar.setBorderColor(borderColorBar);
            graphDatasetBar.setHoverBorderColor(hoverBorderColorBar);
            listaGraphDatasetBar[contador] = graphDatasetBar;

            contador++;
            contadorColor++;
            if (contadorColor >= REGISTROS_COLOR) {
                contadorColor = 1;
            }

        }
        graphBarChart.setDatasets(listaGraphDatasetBar);
        graphBarChart.setLabels(labelsBar);
        graphBean.setBarchart(graphBarChart);
        return graphBean;
    }

    protected String setGraphDescripcionByMtrSector(MtrSector mtrSector) {
        return mtrSector.getId().toString();
    }

    protected String setGraphPieChartTituloByMtrSector() {
        return "MtrSector";
    }

    protected String setGraphBarChartTituloByMtrSector() {
        return "MtrSector";
    }

}
