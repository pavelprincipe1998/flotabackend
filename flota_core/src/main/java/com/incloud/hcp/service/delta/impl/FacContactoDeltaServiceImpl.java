/*
 * Project home: https://github.com/jaxio/celerio-angular-quickstart
 * 
 * Source code generated by Celerio, an Open Source code generator by Jaxio.
 * Documentation: http://www.jaxio.com/documentation/celerio/
 * Modificado por CARLOS BAZALAR
 * Email: cbazalarlarosa@gmail.com
 * Template pack-angular:src/main/java/dto/EntitydeltaDTOService.java.e.vm
 */
package com.incloud.hcp.service.delta.impl;

import com.incloud.hcp.domain.FacContacto;
import com.incloud.hcp.domain.response.FacContactoResponse;
import com.incloud.hcp.service.delta.FacContactoDeltaService;
import com.incloud.hcp.service.impl.FacContactoServiceImpl;
import com.incloud.hcp.service.support.PageRequestByExample;
import org.apache.poi.hssf.usermodel.HSSFRow;
import org.apache.poi.hssf.usermodel.HSSFSheet;
import org.apache.poi.ss.usermodel.Cell;
import org.springframework.data.domain.Example;
import org.springframework.data.domain.ExampleMatcher;
import org.springframework.data.domain.Sort;
import org.springframework.stereotype.Service;
import org.springframework.transaction.annotation.Propagation;
import org.springframework.transaction.annotation.Transactional;

import javax.persistence.criteria.CriteriaBuilder;
import javax.persistence.criteria.CriteriaQuery;
import javax.persistence.criteria.Predicate;
import javax.persistence.criteria.Root;
import java.util.List;

/**
 * A simple DTO Facility for FacContacto.
 */
@Service
@Transactional(propagation = Propagation.REQUIRED, rollbackFor = Exception.class)
public class FacContactoDeltaServiceImpl extends FacContactoServiceImpl implements FacContactoDeltaService {

    /**************************/
    /* Metodos Personalizados */
    /**************************/

    /***********************/
    /* Metodos de Busqueda */
    /***********************/

    protected Sort setFindAll(Sort sort) {
        sort = Sort.by(
                new Sort.Order(Sort.Direction.ASC, "usuario")
        );
        return sort;
    }

    protected Sort setFind(FacContacto req, ExampleMatcher matcher, Example<FacContacto> example, Sort sort) {
        sort = Sort.by(
                new Sort.Order(Sort.Direction.ASC, "usuario")
        );
        return sort;
    }

    protected void setFindPaginated(PageRequestByExample<FacContacto> req, ExampleMatcher matcher, Example<FacContacto> example) {
        return;
    }

    protected List<Predicate> setAdicionalDeltaPredicate(List<Predicate> predicates, FacContactoResponse bean, CriteriaBuilder cb,
            CriteriaQuery<FacContacto> query, Root<FacContacto> root) {

        FacContacto entity = bean.getBean();
        //Ejemplo
        /*
        if (bean.get<ClaseManytoOne>Condicion().equals(ConditionEnum.PERSO.getEstado())) {
            if (Optional.ofNullable(entity.get<VariableManytoOne>()).isPresent()) {
                Join<FacContacto, <ClaseManytoOne>> from<ClaseManytoOne> = countRoot.join("<variableManytoOne>", JoinType.INNER);
            
                if (Optional.ofNullable(entity.get<VariableManytoOne>().get<Atributo>()).isPresent()) {
                    Join<FacContacto, <ClaseManytoOne>> from<ClaseManytoOne> = countRoot.join("<variableManytoOne>", JoinType.INNER);
                    Predicate thirdCondition = cb.equal(from<ClaseManytoOne>.get("<Atributo>"), entity.get<ClaseManytoOne>().get<Atributo>());
                    predicates.add(thirdCondition);
                }
                
            }
        }
        */
        return predicates;
    }

    protected Root<FacContacto> setAdicionalDeltaTotalPredicate(FacContactoResponse bean, Root<FacContacto> countRoot) {
        //Ejemplo
        /*
        if (bean.get<ClaseManytoOne>Condicion().equals(ConditionEnum.PERSO.getEstado())) {
            if (Optional.ofNullable(entity.get<VariableManytoOne>()).isPresent()) {
                Join<FacContacto, <ClaseManytoOne>> from<ClaseManytoOne> = countRoot.join("<variableManytoOne>", JoinType.INNER);
            }
        }
        */
        return countRoot;
    }

    /****************/
    /* METODOS CRUD */
    /****************/

    protected String setValidacionesPrevias(FacContacto bean) throws Exception {
        String mensaje = "";
        return mensaje;
    }

    protected FacContacto setCreate(FacContacto bean) throws Exception {
        return bean;
    }

    protected void setSave(FacContacto dto) throws Exception {
        return;
    }

    protected void setDelete(Integer id) throws Exception {

    }

    protected void setDeleteAll() throws Exception {

    }

    /************************/
    /* METODOS CRUD Masivos */
    /************************/

    protected FacContacto setUploadExcel(Cell currentCell, FacContacto facContacto, int contador) throws Exception {
        facContacto = super.setUploadExcel(currentCell, facContacto, contador);
        return facContacto;
    }

    protected String setSaveMasivo(FacContacto dto) throws Exception {
        return "";
    }

    protected List<FacContacto> setBeforeDeleteMasivo(List<FacContacto> listaDto) throws Exception {
        return listaDto;
    }

    protected void setAfterDeleteMasivo() throws Exception {
        return;
    }

    /*****************************/
    /* Metodos que generan Excel */
    /*****************************/

    protected void setDownloadExcelItem(FacContacto bean, HSSFRow dataRow) {

    }

    protected void setDownloadExcel(HSSFSheet sheet) {

    }

    /*****************/
    /* Otros Metodos */
    /*****************/

}
