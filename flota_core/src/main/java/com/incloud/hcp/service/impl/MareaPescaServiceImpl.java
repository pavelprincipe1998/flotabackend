/*
 * Project home: https://github.com/jaxio/celerio-angular-quickstart
 * 
 * Source code generated by Celerio, an Open Source code generator by Jaxio.
 * Documentation: http://www.jaxio.com/documentation/celerio/
 * Modificado por CARLOS BAZALAR
 * Email: cbazalarlarosa@gmail.com
 * Template pack-angular:src/main/java/dto/EntityDTOService.java.e.vm
 */
package com.incloud.hcp.service.impl;

import com.incloud.hcp.common.enums.AppParametriaLabelEnum;
import com.incloud.hcp.common.enums.AppParametriaModuloEnum;
import com.incloud.hcp.common.excel.ExcelDefault;
import com.incloud.hcp.common.graph.GraphBarChart;
import com.incloud.hcp.common.graph.GraphBean;
import com.incloud.hcp.common.graph.GraphDataset;
import com.incloud.hcp.common.graph.GraphPieChart;
import com.incloud.hcp.domain.*;
import com.incloud.hcp.domain.response.MareaPescaResponse;
import com.incloud.hcp.exception.ServiceException;
import com.incloud.hcp.repository.delta.*;
import com.incloud.hcp.service.MareaPescaService;
import com.incloud.hcp.service._framework.bean.BeanCargaMasivoDTO;
import com.incloud.hcp.service._framework.bean.BeanListaMasivoDTO;
import com.incloud.hcp.service._framework.impl.JPACustomServiceImpl;
import com.incloud.hcp.service.delta.MtrEmbarcacionDeltaService;
import com.incloud.hcp.service.delta.MtrHorometroDeltaService;
import com.incloud.hcp.service.delta.MtrTipoEventoDeltaService;
import com.incloud.hcp.service.requireNew.MareaPescaRequireNewService;
import com.incloud.hcp.service.support.PageRequestByExample;
import com.incloud.hcp.utils.Constants;
import com.incloud.hcp.utils.PredicateUtils;
import org.apache.commons.beanutils.BeanUtils;
import org.apache.commons.lang.StringUtils;
import org.apache.poi.hssf.usermodel.HSSFRow;
import org.apache.poi.ss.usermodel.Cell;
import org.apache.poi.ss.usermodel.CellStyle;
import org.apache.poi.ss.usermodel.Row;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.context.i18n.LocaleContextHolder;
import org.springframework.data.domain.Example;
import org.springframework.data.domain.ExampleMatcher;
import org.springframework.data.domain.Sort;
import org.springframework.stereotype.Service;
import org.springframework.transaction.annotation.Propagation;
import org.springframework.transaction.annotation.Transactional;

import javax.persistence.criteria.CriteriaBuilder;
import javax.persistence.criteria.Predicate;
import javax.persistence.criteria.Root;
import java.math.BigDecimal;
import java.util.ArrayList;
import java.util.List;
import java.util.Optional;

/**
 * A simple DTO Facility for MareaPesca.
 */
@Service
@Transactional(propagation = Propagation.REQUIRED, rollbackFor = Exception.class)
public abstract class MareaPescaServiceImpl extends JPACustomServiceImpl<MareaPescaResponse, MareaPesca, Integer> implements MareaPescaService {

    protected final String NAME_SHEET = "MareaPesca";
    protected final String CONFIG_TITLE = "com/incloud/hcp/excel/MareaPescaExcel.xml";
    private final Integer REGISTROS_COLOR = 10;

    @Autowired
    protected AppParametriaDeltaRepository appParametriaDeltaRepository;

    @Autowired
    protected MareaPescaDeltaRepository mareaPescaDeltaRepository;

    @Autowired
    protected MareaPescaRequireNewService mareaPescaRequireNewService;

    @Autowired
    protected MtrEmbarcacionDeltaService mtrEmbarcacionDeltaService;

    @Autowired
    protected MtrEmbarcacionDeltaRepository mtrEmbarcacionDeltaRepository;

    @Autowired
    protected MtrTipoEventoDeltaService mtrTipoEventoDeltaService;

    @Autowired
    protected MtrTipoEventoDeltaRepository mtrTipoEventoDeltaRepository;

    @Autowired
    protected MtrHorometroDeltaService mtrHorometroDeltaService;

    @Autowired
    protected MtrHorometroDeltaRepository mtrHorometroDeltaRepository;

    /*****************************/
    /* Metodos de Busqueda       */
    /*****************************/

    protected final ExampleMatcher setAbstractFind(ExampleMatcher matcher) {
        matcher = ExampleMatcher.matching() //
                .withMatcher(MareaPesca_.numeroMarea.getName(), match -> match.ignoreCase().startsWith());
        return matcher;
    }

    protected abstract Sort setFindAll(Sort sort);

    protected abstract Sort setFind(MareaPesca req, ExampleMatcher matcher, Example<MareaPesca> example, Sort sort);

    protected abstract void setFindPaginated(PageRequestByExample<MareaPesca> req, ExampleMatcher matcher, Example<MareaPesca> example);

    protected final MareaPesca setObtenerBeanResponse(MareaPescaResponse bean) {
        return bean.getBean();
    }

    protected final Class<MareaPesca> setObtenerClassBean() {
        return MareaPesca.class;
    }

    protected List<Predicate> setAbstractPredicate(MareaPescaResponse bean, CriteriaBuilder cb, Root<MareaPesca> root) {
        List<Predicate> predicates = new ArrayList<>();
        MareaPesca entity = bean.getBean();
        /* Obtener condiciones */
        PredicateUtils.addPredicates(predicates, bean.getIdCondicion(), "id", entity.getId(), cb, root);
        PredicateUtils.addPredicates(predicates, bean.getNumeroMareaCondicion(), "numeroMarea", entity.getNumeroMarea(), cb, root);
        PredicateUtils.addPredicates(predicates, bean.getValorHorometroCondicion(), "valorHorometro", entity.getValorHorometro(), cb, root);
        //PredicateUtils.addPredicates(predicates, bean.getMtrEmbarcacionCondicion(), "mtrEmbarcacion", entity.getMtrEmbarcacion(), cb, root);
        //PredicateUtils.addPredicates(predicates, bean.getMtrTipoEventoCondicion(), "mtrTipoEvento", entity.getMtrTipoEvento(), cb, root);
        //PredicateUtils.addPredicates(predicates, bean.getMtrHorometroCondicion(), "mtrHorometro", entity.getMtrHorometro(), cb, root);
        /* Obtener valores de Lista */
        PredicateUtils.addPredicatesListValorPrimitivo(predicates, "id", bean.getIdList(), cb, root);
        PredicateUtils.addPredicatesListValorPrimitivo(predicates, "numeroMarea", bean.getNumeroMareaList(), cb, root);
        PredicateUtils.addPredicatesListValorPrimitivo(predicates, "valorHorometro", bean.getValorHorometroList(), cb, root);
        //PredicateUtils.addPredicatesListValorBean(predicates, "mtrEmbarcacion", bean.getMtrEmbarcacionList(), cb, root);
        //PredicateUtils.addPredicatesListValorBean(predicates, "mtrTipoEvento", bean.getMtrTipoEventoList(), cb, root);
        //PredicateUtils.addPredicatesListValorBean(predicates, "mtrHorometro", bean.getMtrHorometroList(), cb, root);
        return predicates;
    }

    /****************/
    /* METODOS CRUD */
    /****************/

    protected final String validacionesPrevias(MareaPesca bean) throws Exception {
        String mensaje = "";
        if (!Optional.of(bean.getNumeroMarea()).isPresent()) {
            String msg = this.messageSource.getMessage("message.mareaPesca.numeroMarea.requerido", null, LocaleContextHolder.getLocale());
            mensaje += "* " + msg + "<br/>";
        }
        String msg = this.setValidacionesPrevias(bean);
        if (StringUtils.isNotBlank(msg)) {
            mensaje += "* " + msg + "<br/>";
        }
        return mensaje;
    }

    protected String setValidacionesPrevias(MareaPesca bean) throws Exception {
        String mensaje = "";
        return mensaje;
    }

    protected String validacionesPreviasCreate(MareaPesca bean) throws Exception {
        String msg = null;
        MareaPesca validar = null;
        return msg;
    }

    protected String validacionesPreviasSave(MareaPesca bean) throws Exception {
        String msg = null;
        MareaPesca validar = null;
        return msg;
    }

    protected MareaPesca completarDatosBean(MareaPesca bean) throws Exception {
        BigDecimal data = new BigDecimal(0.00);
        if (Optional.ofNullable(bean.getValorHorometro()).isPresent()) {
            bean.setValorHorometro(bean.getValorHorometro().setScale(2, BigDecimal.ROUND_HALF_UP));
        }
        bean = this.setCompletarDatosBean(bean);
        return bean;
    }

    protected MareaPesca setCompletarDatosBean(MareaPesca bean) throws Exception {
        return bean;
    }

    protected final MareaPesca setAbstractCreate(MareaPesca dto) throws Exception {
        MareaPesca bean = new MareaPesca();
        bean = (MareaPesca) BeanUtils.cloneBean(dto);
        return bean;
    }

    /************************/
    /* METODOS CRUD Masivos */
    /************************/

    protected MareaPesca setUploadExcel(Cell currentCell, MareaPesca mareaPesca, int contador) throws Exception {
        Double valorDouble = new Double(0);
        BigDecimal valorDecimal = new BigDecimal(0);
        Boolean valorBoolean = new Boolean(true);
        String valorCadena = "";
        switch (contador) {
        case 1:
            try {
                valorCadena = currentCell.getStringCellValue();
                if (valorCadena.length() > 100) {
                    throw new ServiceException("Valor Campo numeroMarea contiene mas de 100 caracter(es)");
                }
                mareaPesca.setNumeroMarea(valorCadena);
            } catch (Exception e) {
                throw new ServiceException("Valor Campo numeroMarea está en formato incorrecto");
            }
            break;
        case 2:
            try {
                valorDecimal = new BigDecimal(currentCell.getNumericCellValue());
                mareaPesca.setValorHorometro(valorDecimal);
            } catch (Exception e) {
                throw new ServiceException("Valor Campo valorHorometro está en formato incorrecto");
            }
            break;
        default:
            break;
        }
        return mareaPesca;
    }

    protected AppParametria setObtenerRegistroConfiguracionUploadExcel() {
        AppParametria appParametriaData = this.appParametriaDeltaRepository.getByModuloAndLabelAndStatus(AppParametriaModuloEnum.CARGA_EXCEL.getEstado(),
                AppParametriaLabelEnum.INICIO_REGISTRO_DATA.getEstado(), Constants.ESTADO_ACTIVO);
        return appParametriaData;
    }

    protected MareaPesca setInicializarBeanUpdateExcel() {
        MareaPesca bean = new MareaPesca();
        bean.setId(null);
        return bean;
    }

    protected final Integer setObtenerId(MareaPesca bean) {
        return bean.getId();
    }

    /************************/
    /* Instancia de Bean    */
    /************************/

    protected final MareaPesca createInstance() {
        MareaPesca mareaPesca = new MareaPesca();
        return mareaPesca;
    }

    protected final BeanCargaMasivoDTO<MareaPesca> createInstanceMasivoDTO() {
        BeanCargaMasivoDTO<MareaPesca> beanCargaMasivoDTO = new BeanCargaMasivoDTO<MareaPesca>();
        return beanCargaMasivoDTO;

    }

    protected final BeanListaMasivoDTO<BeanCargaMasivoDTO<MareaPesca>> createInstanceListaMasivoDTO() {
        BeanListaMasivoDTO<BeanCargaMasivoDTO<MareaPesca>> beanCargaMasivoDTOBeanListaMasivoDTO = new BeanListaMasivoDTO<BeanCargaMasivoDTO<MareaPesca>>();
        return beanCargaMasivoDTOBeanListaMasivoDTO;
    }

    /*****************************/
    /* Metodos que generan Excel */
    /*****************************/

    protected String devuelveNombreSheet() {
        return this.NAME_SHEET;
    }

    protected String devuelveListaHeaderExcelXML() {
        return this.CONFIG_TITLE;
    }

    protected int setAbstractDownloadExcel(MareaPesca bean, HSSFRow dataRow) {
        int contador = 0;
        ExcelDefault.setValueCell(bean.getId(), dataRow.createCell(contador));
        contador++;
        ExcelDefault.setValueCell(bean.getNumeroMarea(), dataRow.createCell(contador));
        contador++;
        ExcelDefault.setValueCell(bean.getValorHorometro(), dataRow.createCell(contador));
        contador++;
        return contador;
    }

    protected int setAbstractDownloadExcelSXLSX(MareaPesca bean, Row dataRow, List<CellStyle> cellStyleList) {
        int contador = 0;
        ExcelDefault.setValueCell(bean.getId(), dataRow.createCell(contador), "I", cellStyleList);
        contador++;
        ExcelDefault.setValueCell(bean.getNumeroMarea(), dataRow.createCell(contador), "S", cellStyleList);
        contador++;
        ExcelDefault.setValueCell(bean.getValorHorometro(), dataRow.createCell(contador), "N", cellStyleList);
        contador++;
        return contador;
    }

    protected String setAbstractGenerateInsertExcelSXLSX(MareaPesca bean) {
        String fechaS = "";
        String sqlInsert = "INSERT INTO MAREA_PESCA(";
        sqlInsert = sqlInsert + "MAREA_PESCA_ID" + ", ";
        sqlInsert = sqlInsert + "NUMERO_MAREA" + ", ";
        sqlInsert = sqlInsert + "VALOR_HOROMETRO" + ", ";
        sqlInsert = sqlInsert + "MTR_EMBARCACION_ID" + ", ";
        sqlInsert = sqlInsert + "MTR_HOROMETRO_ID" + ", ";
        sqlInsert = sqlInsert + "MTR_TIPO_EVENTO_ID" + ")";
        sqlInsert = sqlInsert + " VALUES (";
        sqlInsert = sqlInsert + bean.getId() + ", ";
        if (StringUtils.isBlank(bean.getNumeroMarea())) {
            sqlInsert = sqlInsert + "null" + ", ";
        } else {
            sqlInsert = sqlInsert + "'" + bean.getNumeroMarea() + "'" + ", ";
        }
        sqlInsert = sqlInsert + bean.getValorHorometro() + ", ";
        sqlInsert = sqlInsert + bean.getMtrEmbarcacion().getId() + ", ";
        sqlInsert = sqlInsert + bean.getMtrTipoEvento().getId() + ", ";
        sqlInsert = sqlInsert + bean.getMtrHorometro().getId();
        sqlInsert = sqlInsert + " );";
        return sqlInsert;
    }

    /*****************/
    /* Otros Metodos */
    /*****************/

    public Long countByMtrEmbarcacion(MtrEmbarcacion mtrEmbarcacion) {
        return this.mareaPescaDeltaRepository.countByMtrEmbarcacion(mtrEmbarcacion);
    }

    public GraphBean graphByMtrEmbarcacion() {
        List<MtrEmbarcacion> lista = this.mtrEmbarcacionDeltaRepository.findAll();
        if (lista == null || lista.size() <= 0) {
            return null;
        }
        GraphBean graphBean = new GraphBean();

        /* Obteniendo Pie Chart */
        GraphPieChart graphPieChart = new GraphPieChart();
        GraphDataset graphDataset = new GraphDataset();
        GraphDataset[] listaGraphDataset = new GraphDataset[1];
        Long[] data = new Long[lista.size()];
        String[] labels = new String[lista.size()];
        String[] backgroundColor = new String[lista.size()];
        String[] hoverBackgroundColor = new String[lista.size()];
        String[] borderColor = new String[lista.size()];
        String[] hoverBorderColor = new String[lista.size()];
        int contador = 0;
        int contadorColor = 0;
        for (MtrEmbarcacion bean : lista) {
            Long valor = this.mareaPescaDeltaRepository.countByMtrEmbarcacion(bean);
            String descripcion = this.setGraphDescripcionByMtrEmbarcacion(bean);
            data[contador] = valor;
            labels[contador] = descripcion;
            backgroundColor[contador] = Constants.BACKGROUND_COLOR_GRAPH[contadorColor];
            hoverBackgroundColor[contador] = Constants.HOVER_BACKGROUND_COLOR_GRAPH[contadorColor];
            borderColor[contador] = Constants.BACKGROUND_COLOR_GRAPH[contadorColor];
            hoverBorderColor[contador] = Constants.HOVER_BACKGROUND_COLOR_GRAPH[contadorColor];
            contador++;
            contadorColor++;
            if (contadorColor >= REGISTROS_COLOR) {
                contadorColor = 1;
            }
        }
        String titulo = this.setGraphPieChartTituloByMtrEmbarcacion();
        graphDataset.setLabel(titulo);
        graphDataset.setData(data);
        graphDataset.setBackgroundColor(backgroundColor);
        graphDataset.setHoverBackgroundColor(hoverBackgroundColor);
        listaGraphDataset[0] = graphDataset;
        graphPieChart.setDatasets(listaGraphDataset);
        graphPieChart.setLabels(labels);
        graphBean.setPiechart(graphPieChart);

        GraphBarChart graphBarChart = new GraphBarChart();
        GraphDataset[] listaGraphDatasetBar = new GraphDataset[lista.size()];
        String[] labelsBar = new String[1];
        labelsBar[0] = this.setGraphBarChartTituloByMtrEmbarcacion();
        contador = 0;
        contadorColor = 0;
        for (MtrEmbarcacion bean : lista) {
            Long valor = this.mareaPescaDeltaRepository.countByMtrEmbarcacion(bean);
            String descripcion = this.setGraphDescripcionByMtrEmbarcacion(bean);
            GraphDataset graphDatasetBar = new GraphDataset();
            Long[] dataBar = new Long[1];

            String[] backgroundColorBar = new String[1];
            String[] hoverBackgroundColorBar = new String[1];
            String[] borderColorBar = new String[1];
            String[] hoverBorderColorBar = new String[1];

            dataBar[0] = valor;
            backgroundColorBar[0] = Constants.BACKGROUND_COLOR_GRAPH[contadorColor];
            hoverBackgroundColorBar[0] = Constants.HOVER_BACKGROUND_COLOR_GRAPH[contadorColor];
            borderColorBar[0] = Constants.BACKGROUND_COLOR_GRAPH[contadorColor];
            hoverBorderColorBar[0] = Constants.HOVER_BACKGROUND_COLOR_GRAPH[contadorColor];

            graphDatasetBar.setLabel(descripcion);
            graphDatasetBar.setData(dataBar);
            graphDatasetBar.setBackgroundColor(backgroundColorBar);
            graphDatasetBar.setHoverBackgroundColor(hoverBackgroundColorBar);
            graphDatasetBar.setBorderColor(borderColorBar);
            graphDatasetBar.setHoverBorderColor(hoverBorderColorBar);
            listaGraphDatasetBar[contador] = graphDatasetBar;

            contador++;
            contadorColor++;
            if (contadorColor >= REGISTROS_COLOR) {
                contadorColor = 1;
            }

        }
        graphBarChart.setDatasets(listaGraphDatasetBar);
        graphBarChart.setLabels(labelsBar);
        graphBean.setBarchart(graphBarChart);
        return graphBean;
    }

    protected String setGraphDescripcionByMtrEmbarcacion(MtrEmbarcacion mtrEmbarcacion) {
        return mtrEmbarcacion.getId().toString();
    }

    protected String setGraphPieChartTituloByMtrEmbarcacion() {
        return "MtrEmbarcacion";
    }

    protected String setGraphBarChartTituloByMtrEmbarcacion() {
        return "MtrEmbarcacion";
    }

    public Long countByMtrTipoEvento(MtrTipoEvento mtrTipoEvento) {
        return this.mareaPescaDeltaRepository.countByMtrTipoEvento(mtrTipoEvento);
    }

    public GraphBean graphByMtrTipoEvento() {
        List<MtrTipoEvento> lista = this.mtrTipoEventoDeltaRepository.findAll();
        if (lista == null || lista.size() <= 0) {
            return null;
        }
        GraphBean graphBean = new GraphBean();

        /* Obteniendo Pie Chart */
        GraphPieChart graphPieChart = new GraphPieChart();
        GraphDataset graphDataset = new GraphDataset();
        GraphDataset[] listaGraphDataset = new GraphDataset[1];
        Long[] data = new Long[lista.size()];
        String[] labels = new String[lista.size()];
        String[] backgroundColor = new String[lista.size()];
        String[] hoverBackgroundColor = new String[lista.size()];
        String[] borderColor = new String[lista.size()];
        String[] hoverBorderColor = new String[lista.size()];
        int contador = 0;
        int contadorColor = 0;
        for (MtrTipoEvento bean : lista) {
            Long valor = this.mareaPescaDeltaRepository.countByMtrTipoEvento(bean);
            String descripcion = this.setGraphDescripcionByMtrTipoEvento(bean);
            data[contador] = valor;
            labels[contador] = descripcion;
            backgroundColor[contador] = Constants.BACKGROUND_COLOR_GRAPH[contadorColor];
            hoverBackgroundColor[contador] = Constants.HOVER_BACKGROUND_COLOR_GRAPH[contadorColor];
            borderColor[contador] = Constants.BACKGROUND_COLOR_GRAPH[contadorColor];
            hoverBorderColor[contador] = Constants.HOVER_BACKGROUND_COLOR_GRAPH[contadorColor];
            contador++;
            contadorColor++;
            if (contadorColor >= REGISTROS_COLOR) {
                contadorColor = 1;
            }
        }
        String titulo = this.setGraphPieChartTituloByMtrTipoEvento();
        graphDataset.setLabel(titulo);
        graphDataset.setData(data);
        graphDataset.setBackgroundColor(backgroundColor);
        graphDataset.setHoverBackgroundColor(hoverBackgroundColor);
        listaGraphDataset[0] = graphDataset;
        graphPieChart.setDatasets(listaGraphDataset);
        graphPieChart.setLabels(labels);
        graphBean.setPiechart(graphPieChart);

        GraphBarChart graphBarChart = new GraphBarChart();
        GraphDataset[] listaGraphDatasetBar = new GraphDataset[lista.size()];
        String[] labelsBar = new String[1];
        labelsBar[0] = this.setGraphBarChartTituloByMtrTipoEvento();
        contador = 0;
        contadorColor = 0;
        for (MtrTipoEvento bean : lista) {
            Long valor = this.mareaPescaDeltaRepository.countByMtrTipoEvento(bean);
            String descripcion = this.setGraphDescripcionByMtrTipoEvento(bean);
            GraphDataset graphDatasetBar = new GraphDataset();
            Long[] dataBar = new Long[1];

            String[] backgroundColorBar = new String[1];
            String[] hoverBackgroundColorBar = new String[1];
            String[] borderColorBar = new String[1];
            String[] hoverBorderColorBar = new String[1];

            dataBar[0] = valor;
            backgroundColorBar[0] = Constants.BACKGROUND_COLOR_GRAPH[contadorColor];
            hoverBackgroundColorBar[0] = Constants.HOVER_BACKGROUND_COLOR_GRAPH[contadorColor];
            borderColorBar[0] = Constants.BACKGROUND_COLOR_GRAPH[contadorColor];
            hoverBorderColorBar[0] = Constants.HOVER_BACKGROUND_COLOR_GRAPH[contadorColor];

            graphDatasetBar.setLabel(descripcion);
            graphDatasetBar.setData(dataBar);
            graphDatasetBar.setBackgroundColor(backgroundColorBar);
            graphDatasetBar.setHoverBackgroundColor(hoverBackgroundColorBar);
            graphDatasetBar.setBorderColor(borderColorBar);
            graphDatasetBar.setHoverBorderColor(hoverBorderColorBar);
            listaGraphDatasetBar[contador] = graphDatasetBar;

            contador++;
            contadorColor++;
            if (contadorColor >= REGISTROS_COLOR) {
                contadorColor = 1;
            }

        }
        graphBarChart.setDatasets(listaGraphDatasetBar);
        graphBarChart.setLabels(labelsBar);
        graphBean.setBarchart(graphBarChart);
        return graphBean;
    }

    protected String setGraphDescripcionByMtrTipoEvento(MtrTipoEvento mtrTipoEvento) {
        return mtrTipoEvento.getId().toString();
    }

    protected String setGraphPieChartTituloByMtrTipoEvento() {
        return "MtrTipoEvento";
    }

    protected String setGraphBarChartTituloByMtrTipoEvento() {
        return "MtrTipoEvento";
    }

    public Long countByMtrHorometro(MtrHorometro mtrHorometro) {
        return this.mareaPescaDeltaRepository.countByMtrHorometro(mtrHorometro);
    }

    public GraphBean graphByMtrHorometro() {
        List<MtrHorometro> lista = this.mtrHorometroDeltaRepository.findAll();
        if (lista == null || lista.size() <= 0) {
            return null;
        }
        GraphBean graphBean = new GraphBean();

        /* Obteniendo Pie Chart */
        GraphPieChart graphPieChart = new GraphPieChart();
        GraphDataset graphDataset = new GraphDataset();
        GraphDataset[] listaGraphDataset = new GraphDataset[1];
        Long[] data = new Long[lista.size()];
        String[] labels = new String[lista.size()];
        String[] backgroundColor = new String[lista.size()];
        String[] hoverBackgroundColor = new String[lista.size()];
        String[] borderColor = new String[lista.size()];
        String[] hoverBorderColor = new String[lista.size()];
        int contador = 0;
        int contadorColor = 0;
        for (MtrHorometro bean : lista) {
            Long valor = this.mareaPescaDeltaRepository.countByMtrHorometro(bean);
            String descripcion = this.setGraphDescripcionByMtrHorometro(bean);
            data[contador] = valor;
            labels[contador] = descripcion;
            backgroundColor[contador] = Constants.BACKGROUND_COLOR_GRAPH[contadorColor];
            hoverBackgroundColor[contador] = Constants.HOVER_BACKGROUND_COLOR_GRAPH[contadorColor];
            borderColor[contador] = Constants.BACKGROUND_COLOR_GRAPH[contadorColor];
            hoverBorderColor[contador] = Constants.HOVER_BACKGROUND_COLOR_GRAPH[contadorColor];
            contador++;
            contadorColor++;
            if (contadorColor >= REGISTROS_COLOR) {
                contadorColor = 1;
            }
        }
        String titulo = this.setGraphPieChartTituloByMtrHorometro();
        graphDataset.setLabel(titulo);
        graphDataset.setData(data);
        graphDataset.setBackgroundColor(backgroundColor);
        graphDataset.setHoverBackgroundColor(hoverBackgroundColor);
        listaGraphDataset[0] = graphDataset;
        graphPieChart.setDatasets(listaGraphDataset);
        graphPieChart.setLabels(labels);
        graphBean.setPiechart(graphPieChart);

        GraphBarChart graphBarChart = new GraphBarChart();
        GraphDataset[] listaGraphDatasetBar = new GraphDataset[lista.size()];
        String[] labelsBar = new String[1];
        labelsBar[0] = this.setGraphBarChartTituloByMtrHorometro();
        contador = 0;
        contadorColor = 0;
        for (MtrHorometro bean : lista) {
            Long valor = this.mareaPescaDeltaRepository.countByMtrHorometro(bean);
            String descripcion = this.setGraphDescripcionByMtrHorometro(bean);
            GraphDataset graphDatasetBar = new GraphDataset();
            Long[] dataBar = new Long[1];

            String[] backgroundColorBar = new String[1];
            String[] hoverBackgroundColorBar = new String[1];
            String[] borderColorBar = new String[1];
            String[] hoverBorderColorBar = new String[1];

            dataBar[0] = valor;
            backgroundColorBar[0] = Constants.BACKGROUND_COLOR_GRAPH[contadorColor];
            hoverBackgroundColorBar[0] = Constants.HOVER_BACKGROUND_COLOR_GRAPH[contadorColor];
            borderColorBar[0] = Constants.BACKGROUND_COLOR_GRAPH[contadorColor];
            hoverBorderColorBar[0] = Constants.HOVER_BACKGROUND_COLOR_GRAPH[contadorColor];

            graphDatasetBar.setLabel(descripcion);
            graphDatasetBar.setData(dataBar);
            graphDatasetBar.setBackgroundColor(backgroundColorBar);
            graphDatasetBar.setHoverBackgroundColor(hoverBackgroundColorBar);
            graphDatasetBar.setBorderColor(borderColorBar);
            graphDatasetBar.setHoverBorderColor(hoverBorderColorBar);
            listaGraphDatasetBar[contador] = graphDatasetBar;

            contador++;
            contadorColor++;
            if (contadorColor >= REGISTROS_COLOR) {
                contadorColor = 1;
            }

        }
        graphBarChart.setDatasets(listaGraphDatasetBar);
        graphBarChart.setLabels(labelsBar);
        graphBean.setBarchart(graphBarChart);
        return graphBean;
    }

    protected String setGraphDescripcionByMtrHorometro(MtrHorometro mtrHorometro) {
        return mtrHorometro.getId().toString();
    }

    protected String setGraphPieChartTituloByMtrHorometro() {
        return "MtrHorometro";
    }

    protected String setGraphBarChartTituloByMtrHorometro() {
        return "MtrHorometro";
    }

}
